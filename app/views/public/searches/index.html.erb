<div class="container admin-black-bg p-5">
  <h1 class="mt-5">
    <img src="<%= asset_path('title-searched.png') %>" alt="検索結果">
  </h1>
  <h2 class="mt-5">
    <img src="<%= asset_path('title-searched-artist.png') %>" alt="アーティスト検索結果">
  </h2>
  <hr class="white-hr">
  <% if @artists.any? %>
    <div class="row row-cols-2 row-cols-sm-3 row-cols-md-4 row-cols-lg-5 search-scroll-box-artist">
      <% @artists.each do |artist| %>
        <div class="col text-center">
          <%= link_to artist_path(artist) do %>
            <%= image_tag artist.get_image(60, 60), class: "img-fluid rounded-circle mb-2" %><br>
            <small class="d-block text-muted text-break mb-3">
              <%= truncate(artist.name, length: 30) %>
            </small>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>該当するアーティストはありません</p>
  <% end %>

  <h2 class="mt-5">
    <img src="<%= asset_path('title-searched-user.png') %>" alt="ユーザー検索結果">
  </h2>
  <hr class="white-hr">
    <% if (current_user && !current_user.guest?) || admin_signed_in? %>
      <% if @users.any? %>
        <div class="search-scroll-box">
          <% @users.each do |user| %>
            <p><%= link_to user.name, user_path(user) %></p>
          <% end %>
        </div>
      <% else %>
        <p>該当するユーザーはいません</p>
      <% end %>
    <% else %>
      <p>未ログイン、ゲストログインユーザーはユーザーの検索はできません</p>
    <% end %>

  <h2 class="mt-5">
    <img src="<%= asset_path('title-searched-group.png') %>" alt="サークル検索結果">
  </h2>
  <hr class="white-hr">
  <% if @groups.any? %>
    <div class="search-scroll-box">
      <% @groups.each do |group| %>
        <p><%= link_to group.name, group_path(group) %></p>
      <% end %>
    </div>
  <% else %>
    <p>該当するサークルはありません</p>
  <% end %>
</div>